[project]
name = "app"
version = "0.1.0"
dependencies = [ "langchain", "langchain-core", "langgraph", "langchain-openai", "python-dotenv", "langchain-community", "langchain-chroma", "fastapi", "uvicorn" ]
requires-python = "~=3.12.0"
