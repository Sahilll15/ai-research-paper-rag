[project]
name = "app"
version = "0.1.0"
dependencies = [ "langchain", "langchain-core", "langgraph", "langchain-openai", "python-dotenv", "langchain-chroma", "fastapi", "uvicorn", "langsmith" ]
requires-python = "~=3.12.0"
